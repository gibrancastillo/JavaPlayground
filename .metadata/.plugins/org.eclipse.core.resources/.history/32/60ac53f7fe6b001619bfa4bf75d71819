package com.ccc.messenger.resources;

import javax.ws.rs.Path;

/**
 * This is a sub-resource based class use by root resource(s).
 * The value in the @Path annotation at the class level is only going to have "/", no real value denoting that the path
 * value we want is the one input from the root resource, the one use to call the root resource's method.
 * NOTE that the class level @Path annotation is optional for sub resources.
 * 
 * @author gecasti
 *
 */
@Path("/")
public class CommentResource {
	
	@GET
	public String test() {
		return "sub resource 'CommentResource' test";
	}
}